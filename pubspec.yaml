name: forest_brewery_app
description: Flutter technical test for the Forest Senior Mobile Engineer challenge.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  bloc_concurrency: ^0.3.0
  dio: ^5.10.0
  flutter_bloc: ^9.1.1
  geolocator: ^14.0.3
  get_it: ^9.2.1
  injectable: ^3.0.0
  stream_transform: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  bloc_test: ^10.0.0
  build_runner: ^2.15.1
  flutter_lints: ^6.0.0
  injectable_generator: ^3.0.2
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
